void setup() {
    size(1000,600);
    
}

void draw() {
    if (mousePressed){
        noStroke();
        fill(random(100,155) + mouseY/5, random(100,155) + mouseX/5, 0);
            var randomNum = random(10,100);
           ellipse(mouseX, mouseY, randomNum, randomNum); 
    }
};
